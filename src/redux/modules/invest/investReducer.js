import {} from './investActionTypes';

const initialGlobalState = {};

//************************ REDUCER ************************************

export const investReducer = (state = initialGlobalState, action) => {
  let newState = state;
  switch (action.type) {
    default:
      return state;
  }
};
